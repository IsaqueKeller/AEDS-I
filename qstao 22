//faça um algoritmo que leia um numero inteiro e mostre uma mensagem indicando se este número é par ou ímpar, e se é positivo ou negativo
#include <iostream>
using namespace std;

void funcaogeral (int numero) {
	
		if (numero % 2 == 0 and numero > 0){ //Par e positivo
		cout << "O numero eh par e positivo" << endl;
		}
		else if (numero % 2 != 0 and numero < 0) {
			cout << "O numero eh impar e negativo" << endl; //Impar e negativo
		}
	
	
		if (numero % 2 == 0 and numero < 0) { // Par e negativo
			cout << "O numero eh par e negativo" << endl;
		}
		else if (numero % 2 != 0 and numero > 0) {
			cout << "O numero eh impar e positivo" << endl; //Impar e positivo
		}
		
		else if (numero == 0) {
			cout << "O numero eh nulo" << endl;
		}
}


int main () {
	int a = 0;
	
	cout << "Informe um numero" << endl;
	cin >> a;
	
	funcaogeral(a);
}
