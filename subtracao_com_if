#include <iostream>
using namespace std;

float subtraction () {
	float num1 = 0 , num2 = 0;
	
	cout << "Type in the first number: " << endl;
	cin >> num1;
	
	cout << "Type in the second number: " << endl;
	cin >> num2;
	
	if (num1 < num2) {
		cout << "Invalid numbers, try again" << endl;
		subtraction(); 
}
 else {
 	return num1 - num2;
 }
}

int main () {
	float answer = subtraction ();
	cout << "The answer is: " << answer << endl;
	return 0;
}
